from django.urls import path, include
from blog import views
from rest_framework.routers import DefaultRouter


# app_name = 'blog'


# urlpatterns = [
#
#     # path('', views.List.as_view(), name='list'),
#     # path('<int:pk>/', views.ArticleShow.as_view(), name='show')
# ]